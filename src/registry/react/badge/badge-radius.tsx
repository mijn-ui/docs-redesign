import { Badge } from "@mijn-ui/react";

const BadgeRadius = () => (
  <div className="flex flex-wrap items-center gap-4">
    <Badge radius="default">Badge</Badge>
    <Badge radius="full">Badge</Badge>
  </div>
);

export default BadgeRadius;
