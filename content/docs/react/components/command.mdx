---
title: Command
description: A command palette for quick access to actions and navigation, often used for keyboard shortcuts.
docs: https://cmdk.paco.me
styleSource: https://github.com/mijn-ui/mijn-ui-react/blob/main/packages/theme/src/components/command.ts
---

<ComponentPreview type="react" name="command-example" />

## Installation

<MDXTabs items={["Npm", "Manual"]}>
  <MDXTabsContent value="Npm">
    ```package-install
    @mijn-ui/react-command
    ```
  </MDXTabsContent>
  <MDXTabsContent value="Manual">
    <Steps>
      <Step>
        **Add the Dialog Component**

        To get started, you need to install the dialog component. Follow the link below to install it:

        [Dialog component](/docs/components/dialog)
      </Step>
      <Step>
        **Install Required Dependencies:**
        ```package-install
        cmdk
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project**

        <ReactSourceCodeDisplay path="packages/components/command/src/command.tsx" />
      </Step>
      <Step>
        **Ensure that everything is functioning correctly after installation.**
      </Step>
    </Steps>

  </MDXTabsContent>
</MDXTabs>

## Usage

```tsx
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@mijn-ui/react-command"
```

```tsx
<Command>
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>Search Emoji</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

---

## Examples

### Command Dialog

<ComponentPreview type="react" name="command-dialog" />

---

## Command Style Slots

| **Slot Name**   | **Targeted Component** | **Purpose**                                      |
| --------------- | ---------------------- | ------------------------------------------------ |
| `base`          | `Command`              | Styles the root `Command` component.             |
| `dialogWrapper` | `CommandDialog`        | Styles the command dialog Wrapper.               |
| `dialog`        | `CommandDialog`        | Styles the command dialog.                       |
| `group`         | `CommandGroup`         | Styles the groups inside the command.            |
| `list`          | `CommandList`          | Styles the command list.                         |
| `item`          | `CommandItem`          | Styles individual command items.                 |
| `separator`     | `CommandSeparator`     | Styles separators inside the command list.       |
| `shortcut`      | `CommandShortcut`      | Styles shortcut text inside commands.            |
| `empty`         | `CommandEmpty`         | Styles the state when the command list is empty. |
| `input`         | `CommandInput`         | Styles the input field of the command.           |
